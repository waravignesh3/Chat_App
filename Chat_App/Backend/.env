# ─────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT ONLY — never commit real credentials
# Production vars must be set in Render's Environment tab
# ─────────────────────────────────────────────────────────

NODE_ENV=development
PORT=5000

# Local MySQL (for dev only)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=madhan457
DB_NAME=chatapp
DB_PORT=3306
DB_CONNECTION_LIMIT=10

# CORS
CLIENT_URLS=http://localhost:5173