NODE_ENV=development
PORT=5000

# MySQL Database Configuration
# For local development, use individual parameters:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=9944779374
DB_NAME=chatapp
DB_PORT=3306
DB_CONNECTION_LIMIT=10

# For production (Render, Railway, etc.), use DATABASE_URL instead:
# DATABASE_URL=mysql://user:password@host:port/database
# (If DATABASE_URL is set, it takes precedence over individual DB_* variables)

# Client URLs (comma-separated for CORS)
CLIENT_URLS=http://localhost:5173,http://127.0.0.1:5173,https://chat-app-kappa-blush-85.vercel.app

# Server URL (for client-side configuration)
VITE_SERVER_URL=http://localhost:5000
